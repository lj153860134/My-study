#8004表示注册到zookeeper服务器的支付服务提供者端口号
server:
  port: 8004


#服务别名-----注册到zookeeper服务中心的别名
spring:
  application:
    name: cloud-provider-payment
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db2020?useUnicode=true&charactEncoding=utf-8&useSSL=false
    username: root
    password: lj210910
  cloud:
    #注册到zookeeper地址
    zookeeper:
      connect-string: localhost:2181


mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.lingjian.springcloud.entities  #所有Entity别名类所在包

  #开启驼峰命名匹配
  configuration:
    map-underscore-to-camel-case: true


