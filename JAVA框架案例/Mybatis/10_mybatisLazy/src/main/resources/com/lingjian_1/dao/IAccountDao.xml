<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingjian_1.dao.IAccountDao">

    <!--定义封装account和user的resultMap-->
    <resultMap id="accountUserMap" type="account">
        <id property="id" column="id"></id>
        <result property="uid" column="uid"></result>
        <result property="money" column="money"></result>
        <!--一对一的关系映射 配置分装user的内容
            select属性指定的内容 查询用户的唯一标识
            column-->

        <association property="user" column="uid" javaType="user" select="com.lingjian_1.dao.IUserDao.findById">

        </association>
    </resultMap>

    <!--配置查询所有-->
    <select id="findAll" resultMap="accountUserMap">
        select * from account
    </select>

    <!--根据uid查询单个用户-->
    <select id="findAccountByUid" parameterType="integer" resultType="account">
        select * from account where uid=#{uid}
    </select>

</mapper>