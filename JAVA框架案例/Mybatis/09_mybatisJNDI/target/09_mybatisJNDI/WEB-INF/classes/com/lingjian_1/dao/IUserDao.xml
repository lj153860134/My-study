<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.lingjian_1.dao.IUserDao">
    <!--配置查询所有-->
    <select id="findAll" resultType="com.lingjian_1.domain.User">
        select  * from user
    </select>

    <!--保存用户-->
    <insert id="saveUser" parameterType="com.lingjian_1.domain.User">
        <!--配置插入操作后 获取插入数据的id-->
        <!--第一个id对应实体类 第二个id对应列名-->
        <selectKey keyProperty="id" keyColumn="id" resultType="Integer" order="AFTER">
        select last_insert_id()
        </selectKey>
        insert into user(username,address,sex,birthday) values(#{username},#{address},
        #{sex},#{birthday})
    </insert>

    <!--更新用户-->
    <update id="updateUser" parameterType="com.lingjian_1.domain.User">
        update user set username=#{username},address=#{address},
        sex=#{sex},birthday=#{birthday} where id=#{id}
    </update>

    <!--删除用户-->
    <delete id="deleteUser" parameterType="int">
        delete from user where id=#{id}
    </delete>

    <!--根据id查询单个用户-->
    <select id="findById" parameterType="integer" resultType="com.lingjian_1.domain.User">
        select * from user where id=#{id}
    </select>

    <!--根据name模糊查询-->
    <select id="findByName" parameterType="String" resultType="com.lingjian_1.domain.User">
        select * from user where username like #{username}
--         select * from user where username like ‘%${value}%’
    </select>

    <!--查询总的用户数-->
    <select id="findCount" resultType="int">
        select count(*) from user
    </select>


</mapper>